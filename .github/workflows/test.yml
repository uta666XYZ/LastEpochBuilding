name: Run Tests
on:
  push:
    branches: [ dev ]
  pull_request:
    branches: [ dev ]
  workflow_dispatch:
jobs:
  run_tests:
    runs-on: ubuntu-latest
    container: ghcr.io/pathofbuildingcommunity/pathofbuilding-tests:latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Run tests
        run: busted --lua=luajit --exclude-tags=""
      - name: Report coverage
        continue-on-error: true # Coverage report may fail if the coveralls.io server is overloaded
        run: cd src; luacov-coveralls --repo-token=${{ secrets.github_token }} -e TestData -e Data -e runtime
  check_generated_builds:
    runs-on: ubuntu-latest
    container: ghcr.io/pathofbuildingcommunity/pathofbuilding-tests:latest
    steps:
      - name: Install xmllint and git dependency
        run: apk add libxml2-utils git
      - name: Checkout
        uses: actions/checkout@v4
      - name: Generate Builds
        run: busted --lua=luajit -r generate
      - run: git config --global --add safe.directory $(pwd)
      - name: Check if the generated builds are different
        run: git diff --exit-code spec
  check_modcache:
    runs-on: ubuntu-latest
    container: ghcr.io/pathofbuildingcommunity/pathofbuilding-tests:latest
    steps:
      - name: Install git dependency
        run: apk add git
      - name: Checkout
        uses: actions/checkout@v4
      - name: Regenerate ModCache
        env:
          LUA_PATH: ../runtime/lua/?.lua;../runtime/lua/?/init.lua
          REGENERATE_MOD_CACHE: 1
        run: cd src; luajit HeadlessWrapper.lua
      - run: git config --global --add safe.directory $(pwd)
      - name: Check if the generated ModCache is different
        run: git diff --exit-code src/Data/ModCache.lua
  # The manifest job should only run when preparing a release on a release-* branch
  check_manifest: 
    runs-on: ubuntu-latest
    if: startsWith(github.head_ref, 'release-')
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Update manifest
        run: python3 update_manifest.py --quiet --in-place
      - name: Check if the generated manifest is different
        run: git diff --exit-code manifest.xml
